# Si nginx sirve solo archivos de `dist`, /api/visits devuelve HTML y el contador falla.
# Opción A (recomendada): todo el tráfico al proceso Node (PM2 + server/static-serve.mjs)
server {
    listen 443 ssl;
    server_name tu-dominio.com;

    location / {
        proxy_pass http://127.0.0.1:4173;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Opción B: solo la API al Node y el resto estático (más trabajo al actualizar dist)
# location /api/visits {
#     proxy_pass http://127.0.0.1:4173/api/visits;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
# }
